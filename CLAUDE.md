# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

# 오케스트레이션 규칙

**적용 범위(중요): 이 섹션은 메인 대화의 오케스트레이터에게만 적용된다. 당신이 서브에이전트(Task/Agent로 스폰된 deep-reasoner/default-worker/task-worker 등)라면 이 섹션 전체를 무시하고 받은 작업을 자신이 직접 수행하라. 서브에이전트가 Agent 도구로 작업을 재위임하는 것은 금지한다 — 재위임 연쇄는 산출물 없이 토큰만 소모한다 (2026-07-07 실제 발생).**

메인 모델(Fable)은 오케스트레이터다. 직접 코드를 수정하지 않는다.
작업을 계획하고, 쪼개고, 서브에이전트에 배분하고, 결과를 종합하는 역할만 한다.

## 작업 배분

- **deep-reasoner** (Opus, effort xhigh): 아키텍처 설계, 복잡한 디버깅, 알고리즘 판단 등 무거운 추론
- **default-worker** (Sonnet, effort high, project 메모리 축적): 기능 구현, 보일러플레이트, 테스트, 리팩터링 등 일반 코드 작업
- **task-worker** (Haiku, effort 미지원 모델): 오타 수정, 파일 정리, 단순 반복 작업 등 가벼운 잡무

effort는 `.claude/agents/*.md` frontmatter에서 에이전트별로 고정돼 있고, 스폰 시 프롬프트로 바꿀 수 없다 (조정하려면 frontmatter 수정). 대량 보일러플레이트처럼 사고 깊이가 불필요한 작업을 default-worker에 시킬 때 비용을 더 줄이려면 frontmatter의 effort를 일시적으로 medium으로 낮출 수 있다.

## 원칙

- 비싼 모델은 판단에만, 싼 모델은 실행에 쓴다.
- 오케스트레이터의 컨텍스트는 가볍게 유지한다. 긴 파일 읽기와 탐색은 서브에이전트에 위임한다.
- worker 스폰 시 프롬프트 첫 줄에 "직접 수행, Agent 재위임 금지"를 명시하고, 스폰 후 1~2분 내 실제 산출물이 나오는지 확인한다.
- 중요한 판단은 codex와 deep-reasoner에 병렬로 독립 검토시키고, 서로의 답을 보여주지 않은 채 두 답을 받아 종합한다. codex는 **절대경로로 실행**: `~/.npm-global/bin/codex exec` (PATH 추가가 `.zshrc`에만 있어 Claude의 비대화형 셸에서는 `which codex`가 실패한다 — 없다고 판단하지 말 것. ChatGPT 계정으로 로그인돼 있음).
- 예외 — 다음 두 경우는 오케스트레이터가 직접 처리해도 된다 (완료 후 검증 명령은 동일하게 실행):
  - (a) 한 줄급 수정: 위임 오버헤드가 작업 자체보다 큰 경우.
  - (b) 진단·검토 과정에서 관련 코드를 이미 전부 읽었고 예상 diff가 약 50줄·3파일 이내인 경우: 계약서 작성 비용이 구현 비용과 맞먹고 워커의 재독해가 이중 지출이 된다 (2026-07-11 실측으로 추가).
  - 반대로 대량 보일러플레이트, 병렬 분할 가능한 작업, 오케스트레이터가 아직 읽지 않은 영역은 원칙대로 위임한다.
- 앱 품질이 비용 절감보다 우선한다. 품질/신뢰성 확보에 필요한 지출(예: Apple 개발자 계정 $99/년)은 미루지 말고 필요 시점에 바로 제안할 것.

## 워커 프롬프트 표준 계약 (스폰 시 7요소 필수)

싼 모델의 품질 하한은 프롬프트 구조가 결정한다. **공통 규약(스택 고정, Expo Go 가드레일, 검증 명령, 보고 형식)은 `horang-worker-guide` 스킬이 default-worker에 자동 주입되므로 스폰 프롬프트에 중복 기재하지 말 것** — 프롬프트에는 작업 고유 사항(2·3·4번 + 작업별 특이 가드레일)만 쓴다. 워커 스폰 프롬프트에 반드시 포함:

1. 첫 줄: "직접 수행하라. Agent 도구로 재위임 절대 금지."
2. 필독 문서를 **섹션 번호까지** 지정 (예: "설계.md §1.3, §4.5"). 문서 전체를 읽게 하지 말 것.
3. 산출물 명세: 파일 경로·인터페이스(플레이스홀더, 스키마, 시그니처)를 계약으로 고정.
4. 파일 소유권 경계: 수정 가능/금지 목록 명시 (병렬 워커 충돌 방지의 핵심).
5. 검증 명령 목록: 실행할 명령을 그대로 적시 (예: `npx tsc --noEmit`, `npx expo export`).
6. 완료 보고 형식 강제: **"스펙과 다르게 판단한 것" 섹션 의무화.** 약한 모델의 최대 리스크는 틀리는 게 아니라 조용히 틀리는 것 — 편차 보고를 강제해 오케스트레이터가 잡는다.
7. 모호하면 중단·보고: 임의 해석 금지, 선택지를 제시하게 할 것.

## 검증자 분리

중요 구현 완료 시 구현 워커의 자기검토에 의존하지 않는다. 신선한 컨텍스트의 별도 검증자(Haiku/Sonnet)를 스폰해 "지시서의 검증 항목을 실제로 통과하는지"만 확인시킨다. 구현자와 검증자는 같은 에이전트를 재사용하지 않는다 (신선한 눈이 자기검토보다 잘 잡는다).

## 실패 사례 체크리스트 (재발 방지 — 사고가 나면 여기에 추가)

- **재위임 연쇄** (2026-07-07): 워커가 Agent로 재위임 → 산출물 없이 토큰 소모. → 표준 계약 1번 + 스폰 후 1~2분 내 산출물 확인으로 방지.
- **실행 환경 호환 미검증** (2026-07-07): npm latest(Expo SDK 57)로 스캐폴드했으나 App Store의 Expo Go는 SDK 54까지만 지원 → 실기기 테스트 불가, 전체 다운그레이드로 복구. → 새 스택 도입/스캐폴드 시 개발 루프의 실행 환경(스토어 앱 버전, OS 최소사양 등)과의 호환을 **먼저** 검증한다. npm "latest"는 실행 환경보다 앞설 수 있다.
- **환경변수 전달 오해** (2026-07-07): 사용자 터미널의 `export`는 Claude의 셸에 전달되지 않는다 (별도 프로세스). → 환경변수는 `.claude/settings.local.json`의 `env`에 저장한다.
- **샌드박스 결과를 실제 컴퓨터의 사실로 오판** (2026-07-09): 전역 설치(`npm install -g`)가 보호된 경로에 써야 하는 상황이었는데 **샌드박스 안에서만 성공**했고, 모델은 그걸 근거로 "설치됐다, 확인된다"를 반복 주장했다. 사용자가 자기 터미널에서 안 된다고 말해도 모델이 자기 관찰로 반박했다. → **설치·권한·전역 상태·파일시스템 변경은 사용자 터미널의 결과가 원본이고, 모델의 셸 결과는 참고치다.** 사용자가 "안 된다"고 하면 반박하지 말고 **환경 차이를 먼저 의심할 것.** 보고할 때도 출처를 밝힌다 — "제 셸에서는 됩니다"(O) / "확인했습니다"(X). `~/.npm-global` prefix 설정은 이 문제의 해결책이었다(홈 디렉터리라 sudo 없이 전역 설치가 된다). 아래 PATH 항목은 이 사고의 원인 규명 과정에서 나온 별건이다.
- **PATH 차이로 CLI 미설치 오판** (2026-07-09): `which codex` 실패를 "미설치"로 판단했으나 실제로는 `~/.npm-global/bin/codex`에 존재 — PATH 추가가 `.zshrc`에만 있어 비대화형 셸이 못 읽은 것. → 사용자가 쓴다고 알려진 CLI는 `which` 실패 시 `~/.npm-global/bin` 등 흔한 설치 경로를 확인하고 절대경로로 실행한다.
- **낡은 문서의 서술을 코드 확인 없이 재생산** (2026-08-25): 설계.md에 "`ALTER TABLE`이 실패하면 앱이 스플래시에 갇힌다"고 적혀 있었고, 모델이 이를 검증 없이 사용자에게 설명하고 **새로 쓰는 문서 두 곳에 다시 옮겨 적었다.** 실제로는 `app/_layout.tsx:53`의 `.catch`가 받아 오류 화면을 그린다(스플래시는 정상 하강). 사용자가 "왜 부팅 에러가 나?"라고 되물어 코드를 열면서 드러났다. 심각도 자체는 같았지만(앱 사용 불가·재설치 외 복구 불가) **틀린 메커니즘이 문서에 두 배로 퍼진 뒤였다.** → **문서의 서술은 근거이지 사실이 아니다.** 특히 ⓐ 실패 경로·에러 동작처럼 재현이 번거로워 아무도 다시 확인 안 한 서술, ⓑ 사용자에게 설명으로 나가는 내용, ⓒ 다른 문서로 옮겨 적는 내용은 **원본 코드 줄을 열어보고 나서** 쓴다. 확인 안 했으면 "문서에는 …라고 돼 있다"로 출처를 밝힐 것.
- **개발자 폰의 OS 설정이 레이아웃 상수를 오염** (2026-08-22, 외부 테스트 직전 발견): 개발 기기의 iOS 텍스트 크기가 **1.235배**(슬라이더 6/7)로 켜져 있어, 실기기에서 측정한 `glyphCenterEm`이 그 배율만큼 부풀려진 **0.82**로 박혔다(참값 0.675 × 1.235 = 0.834). 기본값(1.0) 기기에서는 글자가 위로 떴다. 같은 기기의 **동작 줄이기**(Reduce Motion)는 반대로 꺼져 있어, 켜진 기기에서 Reanimated 애니메이션이 전부 최종값으로 점프해 호랑이 연출·로켓 축포가 통째로 사라졌다. → ⓐ **실측으로 구한 상수는 측정 조건(기기·OS 설정)을 상수 옆에 명시한다.** "실기기에서 쟀다"만으로는 재현이 안 된다. ⓑ OS 설정이 레이아웃에 개입할 경로는 코드에서 차단한다(`allowFontScaling={false}`, `ReducedMotionConfig`). 배려의 반대가 아니라, 배려하려면 먼저 통제가 돼야 한다. ⓒ **기준은 개발자 폰이 아니라 공장 기본값**이다 — iOS 텍스트 크기 기본은 슬라이더 4/7(배율 1.0), 안드로이드도 1.0.
- **증상 범위를 안 물어보고 원인부터 좁힘** (2026-08-26): "테스트 결과 화면에서 뒤로가기 화살표가 진했다가 툭 흐려진다"는 보고에, 방금 그 화면의 헤더 옵션을 고친 직후라 **내 변경이 원인이라는 전제**로 그 화면 코드만 팠다(잠금 때 꽂히는 빈 커스텀 뷰, `setOptions` 병합 타이밍, 스크롤 유무에 따른 내비바 외형 전환까지 가설 3개). 사용자가 "**모든 화면에서 그런다**"고 한 순간 전부 폐기됐다 — 최신 iOS의 원형 유리 뒤로가기 버튼이 그려지는 **OS 기본 동작**이고 아이폰 기본 앱도 동일하다(사용자 확인). **손댈 것이 없다 — 고치면 오히려 시스템과 달라진다.** 화살표가 새로 생겨서 그 구석을 처음 쳐다본 것뿐이었다. → **한 화면에서 보고된 증상은 그 화면 코드를 열기 전에 "다른 화면에서도 나는가 / 다른 앱에서도 나는가"부터 묻는다.** 질문 하나가 가설 3개보다 싸다. 방금 그 파일을 건드렸을 때 이 편향이 가장 세게 걸린다.
- **기기 결함을 코드 결함으로 오진** (2026-08-29, 위 항목이 사흘 만에 재발한 것): 테스트 화면이 통째로 먹통(답 입력칸·스크롤 전부 무반응)이 됐는데, 방금 그 화면을 고친 직후라 또 **내 변경이 원인이라는 전제**로 두 가지를 연달아 지목했다 — `removeClippedSubviews`(내용이 사라지는 증상이 아니었으니 애초에 맞지도 않는 가설), iOS 접근성 크로스 페이드(켜져 있는 건 사실이나 그 설정 그대로 예전엔 잘 돌았다). `expo start -c` + 앱 재시작으로도 안 고쳐졌고 **코드를 커밋 상태로 되돌려도 그대로**였다. 결국 **폰을 재부팅하니 정상**으로 돌아왔다 — 원인은 코드에 없었다. → ⓐ 내가 재현할 수 없는 증상은 **코드보다 실행 환경을 먼저 의심한다**(샌드박스 항목과 같은 계열 — 원본은 사용자 기기다). ⓑ 가장 싼 판별은 가설이 아니라 **known-good으로 되돌려 보기**다. 되돌려도 증상이 남으면 원인은 내 변경 밖이고, 이걸 먼저 했으면 빗나간 가설 둘을 안 세웠다. ⓒ 딸린 사실: **Expo Go의 user.db와 TestFlight 빌드의 user.db는 별개 컨테이너다.** 개발 QA로 Expo Go를 지워도 딸의 학습 기록은 안전하고(반대도 마찬가지), 개발 기기에서 본 학습 상태를 딸의 것으로 착각하지 말 것.

## 기계적 검증 게이트 (.claude/settings.json — 모델의 선의에 의존하지 않는 층)

- 원본 보호: `data/**`, `build/id_map.json`, `단어장 앱 만들기.md`는 Edit/Write가 permissions.deny로 차단됨 (clean.py 등 스크립트 경유 쓰기는 허용).
- 자동 타입체크: 턴 종료 시(Stop/SubagentStop 훅, `.claude/hooks/tsc-check.sh`) 수정된 `.ts/.tsx`가 있으면 `tsc --noEmit`을 1회 실행, 실패 시 모델에게 자동 통보. 워커가 검증을 깜빡해도 시스템이 잡는다. (편집마다 검사하던 방식은 다중 Edit 중간 상태를 잡는 오탐이 잦아 2026-07-11 턴 종료 검사로 이동.)

# 프로젝트: 호랑잉글리시 (Horang English)

**중학생** 대상 영단어 학습 앱 (React Native + Expo). 교육부 지정 어휘 2,416개를 하루 단위 단어장(Day1, Day2...)으로 생성해 암기·복습·테스트하고, 점수를 용돈(Income) 장부와 연결한다. (2026-08-16 정정 — 원래 "고등학생 대상"이라 적혀 있었으나 2026-08-10 제품 방향 확정으로 중학생용 단일 제품이 됐다. 고등학생용은 출시 후 별도 앱으로 분기한다.)

## 필독 문서

- `설계.md` — **구현의 유일한 지침.** SQLite DDL 전문, 원본 정제·파싱 규칙, 콘텐츠 생성 파이프라인, 화면 구조·라우팅·상태관리, 핵심 쿼리, 미결 목록(§6). 코드 작업 전 반드시 해당 섹션을 읽을 것.
- `기술방향.md` — 스택 확정 근거(PWA 반려 사유), 개발 환경 제약, 비용 전략, 가드레일
- `단어장 앱 만들기.md` — 기획서 원문 (화면별 요구사항). **수정 금지 — 사용자의 원본 문서.**

## 명령어

```bash
npx expo start          # Metro 개발 서버 (Expo Go로 iPhone 실기기 테스트, QR 연결)
python3 scripts/clean.py  # 원본 정제 파이프라인: data/voca_data.xlsx → build/ + review/ (인자 없음, 멱등)
```

- 배치 생성 파이프라인(제출~content.db 빌드) 절차는 `/content-pipeline` 스킬 참조 (매 세션 로드 안 됨 — 필요할 때만).

- 테스트·린트는 아직 미설정 (스캐폴드 직후 상태).
- Expo SDK 54 / RN 0.81 / React 19.1 / TypeScript 5.9. 로컬 Node v22.
- iOS 네이티브 빌드는 로컬 불가(iMac 2015, Xcode 14.2 한계) — EAS 클라우드 빌드 사용. 개발 반복은 Expo Go로 무료.

## 아키텍처

**2-DB 구조** (설계.md §1): 읽기 전용 `content.db`(단어·뜻·예문, 사전 빌드해 에셋 번들) + `user.db`(학습 진행·점수·설정, 첫 실행 시 생성). 날짜는 INTEGER epoch day(**디바이스 로컬 자정** 기준 일수, lib/dates.ts `toEpochDay()` 단일 유틸만 사용)로 저장 — 복습 -N일 조회가 정수 비교로 인덱스를 탄다.

**콘텐츠 데이터 흐름** (설계.md §2–3):
```
data/voca_data.xlsx (원본, 수정 금지)
  → scripts/clean.py (정제·파생어 분해·쓰기 파싱·stable_id 발급)
    → build/words.json (words 2417 / lemma_groups 2171 / writing_items 43)
    → build/id_map.json (stable_id 영속 맵 — 재실행 멱등성의 핵심, §3.4)
    → review/*.csv (사람 검수 큐: 목표 영단어 미확정 항목)
  → [검수 승인 후] LLM 뜻·예문 일괄 생성 → content.db 빌드
```

**앱 코드**: expo-router 파일 기반 라우팅(`app/`), 상태관리는 Zustand + 화면 로컬(React Query 등 미도입). 화면 트리·화면별 쿼리 매핑은 설계.md §4, 폴더 지도는 `구조.md`.

## 가드레일

- **Expo Go에 포함되지 않은 서드파티 네이티브 모듈 도입 금지.** 도입하는 순간 dev build(= 유료 Apple 계정)가 강제됨. FlashList도 이 이유로 금지 — 100+행 테이블은 RN 내장 FlatList 윈도잉(보이는 15~20행) + stagger 애니메이션으로 처리 (설계.md §4.5).
- 허용된 네이티브 의존성: expo-sqlite, expo-speech, expo-notifications, react-native-reanimated, react-native-gesture-handler, react-native-view-shot, expo-sharing, react-native-svg (+ expo-router 동반 패키지). **expo-haptics ~15.0.8** 설치 완료(2026-08-11, 머리 롱프레스 1초 신호). **expo-splash-screen ~31.0.13** 설치 완료(2026-08-16, §4-2). 추가 예정: **expo-audio ~1.1.1**(호랑이 사운드 — `expo-av`는 SDK 54에서 deprecated이므로 쓰지 말 것, 사운드 파일 도착 후 설치). 추가 전 `node_modules/expo/bundledNativeModules.json`에 해당 모듈이 있는지 반드시 확인할 것 — 없으면 dev build가 강제된다.
- `data/voca_data.xlsx`·`build/id_map.json`을 임의 수정/삭제하지 말 것 — id_map이 깨지면 앱 업데이트 시 사용자 진행 데이터 참조가 전부 무효화된다.
- TTS 연속 탭 중복 재생 방지: `Speech.stop()` 후 `speak()` 패턴.
- **홈 화면 `ScrollView`는 `react-native-gesture-handler` 것을 쓴다** (`react-native` 것 아님). RN 기본 ScrollView는 gesture-handler에 등록돼 있지 않아 `blocksExternalGesture()`가 **에러도 경고도 없이 조용히 무시**된다. 그러면 머리 "아래로 당기기" Pan과 iOS `UIScrollView`의 내장 pan이 네이티브에서 그냥 경쟁해, **같은 동작인데 어떨 땐 스크롤 어떨 땐 당기기**가 된다 (2026-08-14 실제 발생).

## 핵심 도메인 규칙

- 단어장은 하루 1개만 생성, 전체 Day 간 단어 중복 절대 금지 (미사용 풀에서 랜덤 추출)
- 파생어는 표제어와 동등한 독립 단어 항목 (노출·출제 동일 취급, 쌍 관계는 lemma_group 메타데이터)
- 복습 스케줄: 학습일 기준 -1/-3/-7/-14/-30/-60/-120일 전 단어장
- 테스트는 자기채점(학생이 직접 오답·발음헷갈림 체크), 혼합 출제(단어→뜻/뜻→단어/쓰기 문제), **하루 1회만**(taken_day 기준, 재채점은 허용 — 2026-07-12 확정)
- 발음 기능은 TTS 출력 + 체크 장부만 (녹음 없음)
- 기억 인출 실패 5단계: 우스와이프 증가/좌스와이프 감소 (아이의 자기 표시 — 뱃지·세션 임계값에만 쓰임. **업적의 낯가림 Top10은 이와 별개로 테스트 기준**: 최신 결과가 오답인 단어를 누적 오답 횟수순으로, 외우면 자동 제외 — 혼동 주의)
- Income: 점수별 금액을 표시하고 부모 지급 여부만 체크하는 장부 (결제 연동 없음)

## 제품 방향 (2026-08-10 확정 — 상세는 설계.md §6-15)

**이 앱은 중학생용 단일 제품이다.** 교육부 지정 어휘 2,416개를 **6개월 안에 완주**하는 것 자체가 소구 포인트이므로(하루 20개 × 121일, 주 5일 기준 약 5.6개월) **단어 풀을 확장하지 않는다.**

**고등학생용은 별도 앱**으로 뺀다 — 더 어려운 단어 풀 + 독해 중 만난 단어 직접 추가(LLM 생성). 착수는 **중학생용 출시 후 상품성 검증 뒤**, 이 저장소를 복사해 분기한다. 디자인 2종 병행·테마 전환·브랜치 관리는 전부 폐기.

**최종 목표는 App Store 공개 출시다** (2026-08-15 확정 — 그 전엔 가족 TestFlight 배포까지가 목표였다). 배포는 **내부 → 외부 → 공개 3단계**로 넓히며, 각 단계가 다음 단계의 리허설이다. 내부/외부는 한번 정하면 못 바꾸는 모드가 아니라 같은 앱에 공존하는 두 채널이므로, 같은 빌드를 양쪽에 뿌릴 수 있고 재빌드 없이 승격된다. 단계별 관문은 §9 참조.

목표 변경으로 성격이 달라진 것들: (a) `design/공개출시전-IP체크리스트.md`의 **발동 조건이 충족됐다**(더 이상 연기 대상이 아님 — 다만 딸 시안 리뉴얼로 실질 잔여 위험은 0). (b) 개인정보처리방침·지원 URL이 **필수**가 됐다. (c) 개인(Individual) 계정으로 출시하면 **판매자명이 실명으로 공개**된다 — 조직 계정 전환은 사업자등록+D-U-N-S가 필요하므로 출시 전 결론을 낼 것. (d) **학습 데이터 백업** — `user.db`는 `SQLite.defaultDatabaseDirectory`(iOS `Documents/SQLite`, lib/db.ts)에 있고 백업 제외 플래그가 없어 **iCloud 기기 백업에 이미 포함된다.** 따라서 가장 흔한 "폰 교체 → 백업 복원" 시나리오는 커버돼 있다(2026-08-15 코드 확인 — 그 전엔 "기기 변경 시 전부 소실"로 잘못 기록돼 있었다). 남은 노출면은 **앱 삭제·재설치**, iCloud 백업 OFF, 무료 5GB 초과로 백업이 조용히 실패하는 경우. **추가 대책은 TestFlight 피드백을 보고 결정**(2026-08-15, 사용자 판단) — 실제로 데이터 소실 클레임이 생기는지 확인한 뒤 내보내기·가져오기나 클라우드 동기화를 검토한다.

## 현재 상태 & 다음 단계 (2026-08-16 갱신 — 현재 사실만 기록, 경위·이력은 git log 참조)

0. **인출 습관 시스템** (설계.md §7이 확정 스펙) — 구현 완료, 실기기 QA 대기. 홈 4슬롯 게이지·스트릭, 단어장 세션 트래킹(체류시간 판정), 학습/인출모드(카운트다운 라인바·시간임박 사이렌·타임오버 탭 재시작, 복습 진입 시 인출모드 기본), 슬롯 알림(lib/notifications.ts, 기본 OFF), 습관 보너스 장부(설정 화면에서 금액 편집), 업적 차트 3종(lib/statsQueries.ts). user.db 스키마 버전 '4' (IF NOT EXISTS 멱등 마이그레이션). 판정식·시간 상수는 코드가 원본: app/day/[dayId].tsx 상단 상수, lib/habitQueries.ts.
   - **체류 임계가 갈리는 축은 차수가 아니라 "배지가 이미 의미를 갖느냐"다** (2026-08-24 딸 실사용 피드백 + 복습 편입으로 2차 개편). **오늘 단어장 1·2번째 패스만** 전체 단어수 기준(`×2.5초` / `×1.5초`)이고, **오늘 3·4번째 + 복습 Day의 모든 패스**는 `배지수 × 2초`(최소 3초). 복습 Day는 며칠에 걸쳐 분류가 끝나 첫 패스부터 배지가 의미 있기 때문이며, 덕분에 다 외운 Day는 3초에 통과한다("볼 게 없는 Day를 보상 때문에 붙잡지 않는다"). 패스 차수는 `getTodayPassCount(dayId)` = **오늘 그 Day를 훑은 횟수**(옛 `getTodaySessionCount`는 오늘 완료한 슬롯 수를 셌다 — 축이 달랐다).
   - **타임어택(인출모드 카운트다운)은 오늘·복습 공통 `전체 단어수 × 3초` 고정.** 배지를 쓰지 않는다 — 배지 0의 뜻이 화면마다 정반대이고(오늘=아직 분류 안 함, 복습=다 외웠음), 배지로 길이를 정하면 길이가 학생의 자기표시에 휘둘린다. **카운트다운과 보상 판정은 다른 시계다** — 전자는 순수 집중 장치, 후자는 체류시간. 그리고 카운트다운은 인출모드에서만 렌더되는데 `initialMode: 'retrieval'`을 넘기는 곳은 복습 화면 하나뿐이라, **오늘 단어장은 학습모드로 열려 타임어택이 안 보인다**(토글해야 나온다).
   - **복습이 슬롯 조건에 편입됐다** (2026-08-24 구현 완료, 실기기 QA 대기). 스펙은 **설계.md §7.1~§7.5가 원본**(여기 중복 기재 금지), 경위·처리결과는 §7.6 미결 4번. 구조만: 슬롯 하나를 채우려면 [오늘 단어장] + [오늘의 복습 대상 Day **전부**]가 필요하고 **매 슬롯마다 다시 전부** 해야 한다(하루 4바퀴). 새 테이블 `slot_part` 1행 = "슬롯 S에서 Day D를 인출했다"(조각)이고, **`retrieval_session` 1행은 조각이 전부 모인 "슬롯 완성"으로 의미만 바뀌고 스키마는 그대로다** — 그래서 스트릭·4/4·slot_pass·알림 예약이 한 줄도 안 바뀌었고 과거 기록도 얼어붙어 소급 소멸이 없다. 전구 3상태(꺼짐 / 지지직 / 완전 점등)는 이 두 테이블을 겹쳐 파생한다. **DB 추가는 앞으로도 `CREATE TABLE/INDEX IF NOT EXISTS`만 쓸 것** — `USER_DB_DDL`은 앱을 켤 때마다 실행되는데 `ALTER TABLE ADD COLUMN`에는 `IF NOT EXISTS`가 없어 **2회차 실행부터 `duplicate column name`으로 throw**하고, `execAsync(USER_DB_DDL)`에 try/catch가 없어 DB 초기화가 통째로 실패한다. 그러면 앱이 **"초기화 중 문제가 발생했어요" 오류 화면에 갇히고**(스플래시가 아니다 — 2026-08-25 정정, `app/_layout.tsx:53`의 `.catch`가 받아 :104를 그린다) `initPromise` 캐시 때문에 재시작해도 똑같이 재현된다. 복구는 앱 업데이트나 재설치뿐이고 재설치는 **학습 기록 전부 소실**.
   - **연쇄 복습 버튼은 `replace` + `key={dayId}`가 세트다.** `replace`는 파라미터만 갈아끼워 리마운트가 안 일어나므로 `key` 없이 쓰면 이전 Day의 스크롤·가림·카운트다운·체류 타이머를 그대로 물고 간다. 그리고 **목적지가 오늘 단어장이면 `initialMode`를 넘기지 않는다**(= 학습모드) — 복습을 먼저 끝내고 이 버튼으로 오늘 단어장에 갈 수 있는데, 인출모드로 열면 아직 한 번도 안 본 새 단어가 뜻이 가려진 채 카운트다운과 함께 나온다.
   - **알림 옵트인** (설계.md §9가 확정 스펙) — 구현 완료, **실기기 QA 통과**(2026-08-26 — 노출 조건·설정 딥링크·새 문구·거절 상한까지 확인). **묻는 시점은 성공 직후가 아니라 실패 직후다**(2026-08-26 개편). 앞 시간대를 전부 흘려보내고 **마지막 시간대에 와서야 단어장을 처음 훑은 뒤 홈으로 돌아왔을 때** "전구 미션이 열릴 때랑 테스트 타임에 알림을 보내줄까요?"를 묻는다(`components/home/NotifyOptInOverlay.tsx`). 폐기한 "첫 세션 직후"안의 실패 이유 둘: ⓐ 판정이 `slot_part` 전체 이력이라 **이미 쓰던 사용자는 앱을 켜는 순간 참**이었다("첫 세션"은 신규 설치에서만 성립하는 조건이었다), ⓑ **성공 직후는 알림이 가장 불필요해 보이는 순간**이라 "나는 알림 없어도 되네"를 학습시킨다. 노출 조건은 여전히 데이터 파생 — 오늘 `slot_part`의 `MIN(slot_index)`가 마지막 슬롯과 같은가(`isLateFirstTouchToday()`). 마지막 슬롯 인덱스는 **상수로 박지 말고 `getSlotConfig()`에서 읽는다**(설정에서 바뀌고, QA도 이 값을 옮겨 재현한다). 물어본 기록은 생애 1회(`notify_optin_asked`)가 아니라 **하루 1회**(`notify_optin_asked_day` = epoch day) — 놓친 날은 하루하루 독립된 사건이라, 어제 거절했다고 오늘의 실패까지 침묵하면 안 된다. **iOS 팝업은 앱 생애 한 번뿐이라 [그래]가 두 갈래로 갈린다**: `canEnableNotificationsInApp()`(= `granted || canAskAgain`)이 true면 앱 안에서 켜고, false(과거 거부)면 `/settings?focus=notifications`로 딥링크해 해당 섹션까지 스크롤·하이라이트한다. **`canAskAgain`만 보면 안 된다** — iOS에서 그 값은 status가 undetermined일 때만 true라 "이미 허용됨"도 false가 되고, 그냥 켜면 될 사용자를 설정 화면으로 헛돌린다. 그리고 `notifications_enabled`를 기본 켜짐으로 바꾸는 것으로는 해결이 안 된다(iOS가 보는 건 우리 값이 아니라 자기 권한이라, 스위치는 켜졌는데 알림은 안 오는 거짓말하는 스위치가 된다) — 같은 이유로 설정 화면이 권한 거부를 만났을 때도 스위치를 미리 켜두지 않고 **의사만 메모리(`wantedRef`)에 기억했다가 `AppState` 복귀 때 권한이 실제로 생겼으면 그때 저장**한다(설정 스위치의 Alert에는 `Linking.openSettings()`로 iOS 설정 앱을 여는 [설정 열기] 버튼이 달렸다 — RN 내장이라 새 네이티브 의존성 없음). 홈에서는 예약 테스트 덮개보다 뒤로 물러난다(`testGate !== null && testGate.kind !== 'due'` — `!== null`까지 요구하는 건 병렬 판정의 틈에 [괜찮아]가 눌려 **오늘치 질문이 통째로 소모되는 것**을 막기 위함). **알림의 목적은 "알려주기"뿐**(2026-08-25 사용자 결정) — 재알림·독촉·집중모드 관통 없음, 무시해도 계속 오면 무시하는 습관이 붙어 정말 중요한 알림까지 같이 죽는다. **QA 재현 절차와 제약은 §9.4 참조** — 낮에는 조건을 그냥 만들 수 없어 시간대 설정을 옮겨야 하고, **iOS 팝업은 재설치 외엔 되살릴 수 없으며 재설치는 학습 기록 전소**다. **이름은 "전구 미션"으로 통일**(2026-08-26) — 설정 "시간대 알림"·알림 본문 "인출 타임"·덮개 "용돈 미션"이 전부 **전구 미션**이 됐다(설정 섹션 `전구 미션 알림`). 근거: 세 후보 중 **화면에 실제로 보이는 것은 전구뿐**이다("시간대"·"인출"은 설계 용어, "용돈"은 결과이지 대상이 아니다). 다만 **슬롯 알림 본문에서는 오히려 이름을 뺐다** — `N번째 전구가 열렸어요`뿐이다(제목에 이미 `호랑잉글리시 🐯`가 붙으므로 본문에서 기능 이름을 또 부를 이유가 없다). 이름 통일은 **헷갈릴 자리에만** 쓰는 것이지 모든 자리에 박는 게 아니다. 그리고 **연속 3회 거절하면 덮개를 더 안 띄운다**(`notify_optin_decline_streak`, [그래]를 고르면 0으로 끊김) — 하루 1회의 꼬리를 막는 상한이고, 설정 스위치는 남으므로 길이 막히는 게 아니라 먼저 말 거는 것만 그만두는 것이다. 상세는 설계.md §9.5.
   - **예약 테스트 + 진입 잠금** (설계.md §8이 확정 스펙) — 구현 완료, **딸 실사용 피드백 2차까지 반영**(2026-08-25). 설정한 시각이 지나면 홈 화면에 풀스크린 덮개(`components/home/TestGateOverlay.tsx`)가 떠 테스트 진입 여부를 대신 결정해준다(딸이 "버튼 누르는 결정" 자체를 무서워한다는 피드백). 판정·영속은 `lib/testSchedule.ts`, 잠금은 `app/test/index.tsx`의 `shouldLock`. 2차 피드백으로 바뀐 것: 미루기 3회→**1회**, 설정 화면 명칭을 "테스트 타임"으로 통일(금지어 규칙 폐기 때 덮개·알림만 고치고 남은 잔여물이었다 — **한 기능을 두 이름으로 부르면 같은 기능인 줄 모른다**), 테스트 타임 알림을 **전구 미션 알림 스위치에 종속**(따라서 기본 OFF, 덮개 자체는 스위치와 무관하게 뜬다 / `setTestAlarm()`의 권한 요청도 함께 제거), 결과 화면 뒤로가기 복원. **결과 화면이 갇혔던 원인은 `<Stack.Screen options>`가 `setOptions` 병합이라는 것** — phase마다 옵션을 다르게 거는 화면에서 명시하지 않은 키는 이전 렌더 값이 살아남는다. `headerBackVisible: true`만 켜면 잠금 때 걸어둔 `headerLeft: () => null`이 화살표 자리를 계속 덮으므로 `headerLeft: undefined`까지 **명시적으로** 써야 한다.
     - **이 기능은 허용적인 쪽으로 기울지 않는다** (2026-08-29 확정, 설계.md §8 배경 ⓓ). 딸이 테스트 시각 설정을 요청한 이유부터가 **"적어도 10시 전에는 시험을 준비하게 만드는 장치"**였고, 탈출구([오늘은 넘어갈래])를 없애 달라고 한 뒤 써 보고 **"탈출구가 없는 게 나를 더 가이드해 준다"**고 확인해 줬다. 그러니 **"너무 빡빡하지 않나" 싶어 무르게 만드는 제안(탈출구 추가, 유예 연장, 조건 완화, 덮개가 단어장 진입만 허용 등)은 기본 반려**한다 — 무르게 만들면 장치가 장치이기를 그만둔다. **진짜 탈출구는 화면 안이 아니라 설정에 있다**(테스트 타임을 끄면 된다). 그래서 끄기의 하루 지연이 핵심이다: 계약을 그만두는 건 되지만 불안한 그 순간에 그만두는 건 안 되게 한 것. 덮개가 뜬 뒤 미루기(1회)를 다 쓰면 **앱 전체가 잠기고**(홈이 유일한 관문) 안 본 단어로 테스트를 보게 되는데, 그 낮은 점수가 곧 장치가 전하려는 신호다.
     - **3차 개편 (2026-08-29, 실기기 QA 대기)** — 스펙은 설계.md §8이 원본, 여기 중복 기재 금지. 요지 셋: ⓐ **홈 [테스트] 버튼도 같은 덮개를 거친다**(manual 모드) — 그 전엔 버튼으로 들어가면 바로 나올 수 있었는데, **스스로 하겠다고 나선 쪽에 더 헐거운 계약이 걸리는 게 앞뒤가 안 맞았다.** 이제 진입은 양쪽 다 길게 누르기 + `locked=1`이라 `shouldLock`이 사실상 모든 정상 진입에 걸린다(파라미터 분기는 딥링크 대비로 남겨둠). 단 **오늘 몫을 이미 봤으면 덮개를 건너뛴다** — 볼 게 없으면 1.2초 홀드가 재채점 화면 통행세가 된다. ⓑ 제목이 "테스트 타임!"→**"테스트"**, 본문이 달래기에서 **상금 안내**로 바뀌었다. 시각·금액·문턱은 **전부 데이터에서 읽어 끼운다**(`due`에 `hour` 추가 + `getPrizeRange()`) — 상수로 박으면 설정을 고친 순간 조용히 거짓말이 된다. ⓒ **`test_alarm_enabled` 기본값이 켜짐으로 바뀌었다**(키 없음 = true). 설정에 들어가 켜야만 시작되는 자기구속 장치는 정작 미루는 사람에게 영영 시작되지 않기 때문 — §9 알림 기본값 문제와 같은 구조다. 딸린 결과: `resetTestAlarmConfig()`(QA 도구)가 이제 "켜짐 + 기본 시각"으로 되돌린다.
1. **콘텐츠 완성** — `assets/db/content.db` 실데이터 빌드 완료 (word 2,416 / meaning 3,719 / example 11,411 / writing 43, 2.9MB). 검수·배치 생성·validate 전 항목 통과. 파이프라인 절차는 `/content-pipeline` 스킬 참조.
2. **화면 구현 완료** — 홈·단어장(가리기/스와이프 5단계/단어 셀 탭 TTS/예문 바텀시트)·복습·테스트·용돈 장부·발음 체크 장부(셀프 해소+재체크 자동 복귀)·설정·**자랑하기 리포트**. Expo Go 실기기 구동 확인됨.
3. **자랑하기 리포트** (설계.md §6-14) — 업적 화면 상단 "🎉 자랑하기" → `app/brag`. 성장 지표를 포스터 1장으로 캡처해 카톡 공유(react-native-view-shot + expo-sharing). 히어로=암기 자산 곡선(react-native-svg 라인 차트), 나머지는 2열 그리드에 **우선순위 상위 4개만** 노출. 발사 후 로켓 축포 애니메이션. 문구 톤 원칙: **아이가 조작하는 UI=재미 / 부모가 받아보는 산출물=신뢰·전문**, 부모 지칭은 "부모님".
4. **홈 화면 리뉴얼 진행 중** — 딸이 그린 시안으로 교체. **레이아웃은 절대좌표다** — `components/home/mockupLayout.ts`의 `PLACE`/`place()`가 원본이며, 좌표는 각 에셋을 `design/호랑이잉글리시.png`(1179×3333)에 템플릿 매칭해 얻은 실측값이다(버튼 오차 0.9~1.6/px). `s = screenWidth / 1179` 하나만 곱해 절대배치한다. **단 세로 기준점은 시안이 아니라 안전영역이다**(2026-08-15 확정) — `PLACE.y = 시안 y − 222`이고 캔버스는 `insets.top` 아래에서 시작한다. **시안이 원본인 것은 요소 간 상대 위치·에셋 재단·겹침 관계이고, 화면에서의 절대 세로 위치는 아니다**: 시안 상단 222px는 딸이 상태바 자리로 비워둔 공간인데 그 크기는 기기마다 다른 값(20~62pt)을 한 숫자로 추측한 것이라, 걷어내고 OS가 주는 `insets.top`이 대신하게 했다(시안 절대 y를 그대로 쓰면 눈에 보이는 상단 여백이 기기별 13.7~50.6pt로 3.7배 벌어진다). `PLACE.x`는 화면 왼쪽 기준이라 시안 x 그대로 — **x와 y의 기준이 다른 게 이 파일 최대의 사고 지점.** 인셋은 **`paddingTop`이 아니라 형제 View 높이**로 준다: 캔버스 자식이 전부 `position:'absolute'`라 padding이 자식을 못 밀고 높이만 늘려서 인셋이 통째로 캔버스 **아래** 여백으로 남는다(2026-08-15 실기기 발생). 하단은 인셋을 주지 않는다 — 산맥이 장식 띠라 홈 인디케이터가 얹혀도 무해하고, 여백이 생기면 시안이 끊긴 것처럼 보인다. 튜닝 손잡이는 `TOP_GAP_DESIGN_PX`(기본 0, 안전영역 아래 숨 쉴 틈) 하나뿐. **세로 나열+gap으로는 재현 불가** — 시안에서 잔디가 호랑이 발을 173px 덮는다(겹침 구간 `PLACE` 기준 y 1147–1320 = 시안 1369–1542). 따라서 **잔디를 호랑이보다 나중에 렌더**한다(2026-08-14 정정 — 그 전엔 순서가 뒤바뀌어 호랑이가 잔디 위에 떠 보였다). 잔디 위쪽이 풀잎 끝이라 거의 투명해 순서만 바꾸면 발이 풀 사이로 비치는 시안 모습이 그대로 나온다. 잔디가 위로 올라가 겹치는 **말풍선 탭 영역 하단 16px은 터치가 안 먹지만 무시하기로 했다**(2026-08-15 사용자 판단 — 사람은 말풍선 가운데를 누른다). 그 16px 살리자고 `GrassGauge`를 `pointerEvents` 묶음으로 감싸지 말 것(전엔 루트에 `none`이 있었으나 `none`은 자식까지 막아 전구를 못 누르게 된다). `%`·`gap` 어림잡기로 레이아웃이 무너진 적 있음(2026-08-11, 꽃 아이콘 1.7배). **같은 원칙이 겹침 에셋 전반에 적용된다 — 캔버스를 맞춰 온 에셋이 코드에서 좌표를 맞추는 것보다 언제나 낫다**: 호랑이(body/face/eye-shut, 1060×1111)도 잔디 게이지(`grass-box`/`bulbs` 928×619, `bulb-glow` 272×619)도 (0,0)에 겹치기만 한다. 전구는 `bulb-glow`만 `x = 전구중심 − 135`로 좌우 이동(세로는 전체 높이라 y 보정 없음), 윤곽선 `bulbs.png`는 빛 **위**에 얹어야 켜진 전구도 테두리가 산다. 전구 중심 실측 **205.5/377/544/717**(균등 아님 — `(i+0.5)/4` 공식 금지). **지침은 `design/홈화면-에셋-가이드.md`가 원본**(시안: `design/호랑이잉글리시.png`, 아빠 시안 일체는 `design/고등학생용-보류/`). 확정: 진입점은 태양=설정 / 말풍선=오늘 단어장 / **빨간 점이 뜬 전구=오늘 단어장**(빨간 점은 누르고 싶어지는 표시라 입구로 쓴다, 2026-08-15) / 호랑이 머리=사운드·애니메이션 / 꽃=업적 / **리본=장식(탭 불가)** / 복습·발음체크·테스트. 리본은 원래 "획득물" 진입점이었으나 화면 스펙이 없어 "준비 중" 스텁만 있었고, 미완성 화면은 App Review 가이드라인 2.1(App Completeness)에 걸리므로 **2026-08-21 외부 TestFlight 준비 중 라우팅을 떼고 그림만 남겼다**(딸 시안 하단 구성은 유지). `HomeMenuButtons`의 `MenuItem.route`가 optional이 된 이유가 이것 — route가 없으면 `Pressable`이 아니라 `pointerEvents:'none'` `View`로 렌더한다(onPress 없는 Pressable은 터치만 삼켜 고장 난 버튼으로 보인다). `app/collection/index.tsx` 스텁은 남아 있으나 **도달 불가** — 획득물을 만들 때 `route: '/collection'` 한 줄을 되살리면 다시 연결된다. 시안이 화면보다 32% 길어 **세로 스크롤 허용**(첫 화면에 태양·호랑이·게이지·버튼 3개까지는 보이게). 잔디 게이지는 3층 스택 — `grass-box`(배경+꺼진 전구) → `bulb-glow`(완료 슬롯만) → `bulbs`(윤곽선) → "N일 연속"(코드) → **빨간 점 Pressable(반드시 맨 마지막)**. 마지막이어야 하는 이유: "N일 연속" row가 박스 전체를 덮는 오버레이라, 점이 그 앞에 오면 row가 탭을 가로챈다(나중에 그린 형제가 터치를 먼저 받는다). 순서만 지키면 `pointerEvents`가 필요 없다. 탭 영역은 점(26px=약 8.7pt로 못 누름)이 아니라 **점+전구**(`GrassGauge.HIT`, 전구 윤곽선 실측 y 245~455 기준 약 50×88pt)이고, 위 끝 y 195는 말풍선 탭 영역 아래끝(캔버스 1163)보다 179px 아래라 겹치지 않는다 — 내리려면 그 계산부터 다시 할 것. 에셋은 `tiger_hero.png`(1060×1111, **모든 호랑이 레이어의 기준 캔버스**)·태양·산맥까지 도착. **호랑이는 머리를 분리한다**(쓰다듬기·하품 때문): `tiger-body.png`(몸) + 머리 컨테이너(`tiger-face-*.png` + `tiger-eye-shut.png`). 전부 1060×1111 캔버스 유지 → 좌표 계산 없음. 머리 회전축 = **(532, 285)** = 50.2%/25.7%. 터치 영역 실측(겹침 없음, 103px 여유): **머리 제스처** x 359–697 / y 0–350, **말풍선 탭**(단어장 진입) x 90–970 / y 384–954 — 제스처 5종은 머리 영역 안에서만 잡힌다. 터치 영역은 **애니메이션 컨테이너 밖에 고정**(머리를 따라 움직이면 추적이 꼬임 — 제스처는 누르는 순간 캡처되므로 머리가 빠져나가도 정상 작동). **깜박임은 덮개(`tiger-eye-shut.png`), 표정은 통스왑 — 섞어 쓴다**: 깜박임은 고빈도·저변화라 통스왑 시 윤곽 1px 차이가 계속 떨려 보이고, 하품은 저빈도·고변화라 묻힌다. 그래서 `tiger-face-blink.png`는 만들지 않는다. 덮개는 **색을 칠하지 말고 옆 털을 복사해 덮을 것**(크레용 질감이라 평평한 주황은 경계가 드러남), 페이드 금지(110ms 하드컷). **머리 제스처 4종**(2026-08-14 재설계 — 발견 가능성 우선): **탭1회=4개 중 랜덤**(움찔+랜덤사운드 / 갸우뚱+으응? / 머리내려감+그르렁 / 하품+손톱+어흥, 직전 것 제외) / **아래로당겼다놓기**=머리튕김+꺅~ / 좌우쓰다듬기=머리내려감+그르렁 / 계속쓰다듬기1.5초↑=하품+손톱+어흥. 평상시엔 간헐적 눈깜박만. 근거: **제스처를 모르는 사용자는 쓰다듬기·3연타를 시도하지 않으므로 탭 하나로 전부 발견되게 한다.** 그래서 **탭3연타는 폐기**(탭 랜덤과 판정 충돌 — 빠른 3탭이 "랜덤 3회"인지 "3연타 1개"인지 갈림). **길게누르기도 폐기하고 아래로 당기기로 교체** — 머리 영역에서 세로 스크롤을 포기하기로 결정(머리가 큰 영역이 아님)해 예전에 당기기를 막았던 ScrollView 충돌 제약이 사라졌다. `blocksExternalGesture(scrollRef)`로 스크롤을 이긴다. 당기기는 **시간 조건 없는 거리 기준 슬링샷**(40px 당기면 햅틱으로 장전, 놓으면 튕김) — 1초 유지 방식보다 아이가 바로 이해한다. 머리 튕김 **90px**(목 연장 60 + 상승 90 = 갭 33px). **트리거는 의도적으로 쉬운 것만 골랐다**(가이드 §2-8) — 연출·사운드가 재미를 만들지 제스처의 정교함이 만드는 게 아니다. 폐기한 안과 이유: ⓐ "아래로 당겨 1초 유지"는 멈춤 판정(속도 임계값+디바운스)이 필요하고 **홈이 세로 스크롤이라 ScrollView가 먼저 가로채** `scrollEnabled` 토글까지 필요 → `Gesture.LongPress()`로 대체(제자리 누름은 스크롤을 안 건드림), ⓑ 쓰다듬기 "횟수" 카운팅은 누적거리+판정지연이 필요 → **"시간"으로 대체**(계속 쓰다듬으면 그르렁→하품, 고양이처럼 자연스럽고 "그르렁 3번 뒤 하품" 문제가 원천 소멸), ⓒ 탭 350ms 대기 → 이전 소리 stop 후 재생(기존 TTS 패턴과 동일). 쓰다듬기 Pan은 **좌우 전용**(`activeOffsetX: ±14` + `failOffsetY: ±10` → 수평 ±36°, `tanθ < 10/14`)으로 세로 스크롤과 분리 — 세로 쓰다듬기는 기술적으로 가능하지만 호랑이 위에서 스크롤이 막혀 폐기(호랑이가 첫 화면 큰 영역을 차지 + 시안이 32% 길어 아래를 못 찾게 됨). 45°가 아니라 36°인 이유는 오판의 대가가 비대칭이기 때문(스크롤→쓰다듬기 오판은 짜증, 반대는 다시 하면 됨). 1초 도달 신호는 `expo-haptics`(손가락이 머리를 가려 시각 신호 무용). 이벤트 1은 `yawn`에 감은 눈이 있으므로 덮개를 꺼야 한다. **하품만 유일하게 그림 교체(불연속)라 한 장 스왑으로는 안 된다** — 나머지 3개는 transform이라 저절로 연속. 입 2단계(`yawn1`/`yawn2`, 닫힐 때도 `yawn1` 경유) + 머리 뒤로 젖히는 모션을 겹쳐야 하품으로 읽힌다(타임라인은 가이드 §2-7). 이벤트 4 때문에 **목 연장분 상단이 화면에 드러나므로 둥글게 마감 필수**(60px 연장 + 90px 상승 = 30px 갭). 사운드는 `expo-audio`(Expo Go 번들 포함, `expo-av`는 deprecated), iOS 무음 스위치 대비 `playsInSilentMode: true` 필요. **도착 완료**: `tiger-body` / `tiger-face-normal` / `tiger-eye-shut`(전부 1060×1111, 규격 검증됨 — 목 연장 y224, 얼굴 (359,0)–(697,280), 덮개 (417,103)–(625,138)). **미제작**: `tiger-face-yawn1/yawn2`, `tiger-claws`, 사운드 4종(딸 육성 녹음 권장).
4-1. **손글씨 폰트 적용 완료** (2026-08-11) — `expo-font ~14.0.12`, 현재 `NanumJungHagSaeng.ttf`. **바꾸려면 `lib/fonts.ts` 두 줄만** 고치면 전체 반영된다(후보 폰트 3종이 `assets/fonts/`에 더 있으나 `require`가 없어 번들에 안 들어감). 로딩은 `app/_layout.tsx` 초기화 게이트에 합쳐 깜빡임 없음, 실패해도 앱은 안 막히고 기본 글꼴로 폴백. **버튼 이미지는 글자 없는 버전으로 교체 예정**이라 라벨(복습/발음체크/테스트)·말풍선 3줄·"N일 연속"을 전부 코드에서 폰트로 렌더한다 — 위치·크기는 딸 손글씨 실측값(가이드 §3). 버튼 3개는 글자 크기·위치가 제각각인 게 원본이니 통일 금지.
   **글자 세로 위치는 폰트 지표 상수 2개가 지배한다** (2026-08-14, `lib/fonts.ts`의 `HANDWRITING_METRICS` + `handwritingTop()`). 항목별 보정값을 박지 말 것 — 각 화면에는 **시안에서 잰 잉크 중심**을 그대로 넣고, 폰트 차이는 상수가 흡수한다. ⓐ `lineHeightEm` 1.15 = TTF hhea에서 계산. **`lineHeight`를 `fontSize`(1.0em)로 두면 폰트 자연 줄높이보다 좁아 iOS가 베이스라인을 재배치해 글자가 아래로 밀린다.** ⓑ `glyphCenterEm` **0.65** (TTF 계산값 0.675에서 실기기를 보고 살짝 내림 = 글자를 계산 위치보다 조금 아래에, 2026-08-23). 2026-08-22 정정 이력 — 그 전엔 0.82였고 "TTF로 구하면 틀리니 반드시 실기기를 재라"고 적혀 있었으나 **정반대였다**: TTF가 옳았고, 0.82는 측정한 기기의 iOS 텍스트 크기가 1.235배로 켜져 있어 부풀려진 값이다(0.675 × 1.235 = 0.834로 당시 실측 범위 0.801~0.836과 일치). 다시 잴 일이 생기면 **텍스트 크기 슬라이더 4/7(배율 1.0)** 에서, `allowFontScaling={false}`가 걸려 있는지 확인하고 잰다. **조정 방향: 키우면 글자가 올라가고 줄이면 내려간다.** 이동량이 `Δ × fontSize`라 큰 글자가 더 많이 움직이므로, **한 항목만 어긋나면 이 상수가 아니라 그 항목의 `centerY`를 고칠 것.** ⓒ **`HANDWRITING_TUNED_SCALE` = 1** — 손글씨 전체 크기를 한꺼번에 조절하는 손잡이(폰트 교체 시에나 쓴다). 원래 1.235였다: 홈·스플래시 `fontSize`가 전부 개발 기기(텍스트 크기 1.235배)에서 눈으로 튜닝한 값이라 OS 배율을 막은 뒤 보정이 필요했는데, **신경 쓸 숫자를 남기지 않으려고 2026-08-23에 각 fontSize로 반올림해 흡수시켰다**(87→107 / 233→288 / 150→185 / 133→164 / 167→206 / 95→117 / 26→32, 오차 최대 0.15pt). `centerY`·`TITLE_GAP` 같은 **위치·간격 값은 함께 키우지 않았다.**
   **OS 텍스트 크기 배율 정책**: 절대좌표 화면(홈·스플래시)과 캡처물(자랑하기 카드·차트 라벨)은 `allowFontScaling={false}`로 완전 차단, 흐름 레이아웃 화면(단어장)은 유동 유지하되 `maxFontSizeMultiplier={1.2}` 상한(행 높이 56 고정 때문). 복습·테스트·장부·설정은 아직 미적용.
   **🔥는 별도 요소로 분리했다** — 이모지는 Apple Color Emoji로 대체되는데 ascent가 커서 같은 `<Text>`에 넣으면 줄 전체를 끌어내린다(그 줄만 0.885em, 나머지 0.62em). 이모지는 폰트 지표를 못 믿으니 박스 중앙 정렬 후 실측으로 보정한다(`GrassGauge.FIRE`).
4-2. **앱 아이콘·스플래시 완료** (2026-08-16, 딸 그림) — 아이콘 `assets/icon.png`(1024², 원본 `design/앱아이콘-원본.png` 2048²). **알파 채널을 반드시 제거할 것** — App Store가 알파 있는 아이콘을 반려한다(원본은 알파가 전부 255라 채널만 뗐다). 모서리 둥근 마스크는 네 귀퉁이가 빈 노트지·잔디라 안전. 안드로이드 어댑티브 아이콘(`assets/android-icon-*.png`)은 **아직 Expo 기본값** — 중앙 66% 원으로 잘려 학사모가 날아가므로 정사각형을 그대로 못 쓴다, 안드로이드 낼 때 별도 재단.

     **스플래시는 두 겹이고 이 구분이 최대 사고 지점이다.** ① **네이티브 스플래시** = iOS가 JS 시작 **전에** storyboard로 그린다 → **애니메이션·커스텀 폰트 불가**(`app.json`의 `expo-splash-screen ~31.0.13` 플러그인 설정, Expo Go에서는 확인 불가·EAS 빌드에서만 보인다). ② **`components/AnimatedSplash.tsx`** = JS가 뜬 뒤 DB 초기화 동안. 글씨 쓰는 호랑이 4프레임 + 줄노트 + "호랑잉글리시"(코드 렌더). **Expo Go에서 ②는 그대로 확인된다.** ①→② 전환이 안 보이려면 호랑이가 같은 크기·같은 자리여야 하는데, 플러그인이 `imageWidth × imageWidth` 정사각형에 contain으로 담아 **화면 정중앙**에 놓으므로 결국 "폭 = `imageWidth` pt, 세로중심 = 화면중심"이다. 그래서 **`AnimatedSplash.IMAGE_WIDTH`(393)와 app.json의 `imageWidth`는 같은 숫자여야 한다** — app.json이 TS를 import할 수 없어 자동 동기화가 불가능하니 한쪽만 고치지 말 것. 화면 폭 비례(%)가 아니라 고정 pt인 이유도 이것(①이 고정 dp만 지원 → 비례로 하면 넓은 기기에서 9% 차이로 전환 때 확대돼 보인다). **호랑이를 위아래로 옮기려면 코드가 아니라 프레임 PNG 캔버스에 투명 여백**을 넣어야 한다(①·②가 같이 움직이는 유일한 방법). ①에는 줄·글자가 없다(storyboard 한계) — 200ms 크로스페이드 동안 둘이 함께 떠오르고 호랑이는 제자리라 자연스럽다. 프레임은 `assets/images/splash-1~4.png`(1179×717, 4장 캔버스·내용경계 완전 동일, 투명 배경). 재생은 **왕복 `[0,1,2,3,2,1]` × 130ms** — 연필이 1→4로 갈수록 왼쪽으로 가는데 1→4→1 단순 반복은 팔이 순간이동해 튄다. **글자는 이미지가 아니라 텍스트**(위치 조정이 숫자 한 줄, 어느 해상도에서도 선명). 줄 맞춤은 **글자를 줄에 스냅하는 게 아니라 줄 격자를 글자에 맞춘다**(`NotebookBackground`의 `lineOffset`) — 스냅 방식은 기기마다 호랑이·글자 간격이 11~31pt로 널뛰었고, 줄의 위상은 안 보이지만 간격은 보인다. 세로 조정 손잡이는 `TITLE_GAP`(pt, **아무 값이나 가능**). 초기화 게이트는 **폰트/DB로 분리**했다(`app/_layout.tsx`): 폰트 대기 중엔 `return null`(네이티브 스플래시가 덮음, 여기서 뭘 그리면 기본 글꼴로 그려져 튄다) → 폰트 완료 시 `hideAsync()` + ② 표시 → DB 완료 시 홈. `SPLASH_MIN_MS` 900은 **0으로 두지 말 것** — 초기화가 0.2초에 끝나면 호랑이가 한 획 긋다 말고 사라져 고장 난 것처럼 보인다(한 바퀴 780ms). 진행률 막대는 **의도적으로 안 넣었다**(DB 복사·폰트 로드 모두 진행률을 안 알려줘 가짜가 된다 — 애니메이션 자체가 인디케이터).
5. 프로젝트는 **Expo SDK 54 고정** (스토어 Expo Go 호환 — 상세는 horang-worker-guide 스킬). 시스템 Python 3.9. anthropic SDK 0.116.0 --user 설치됨. 추가 네이티브 의존성: react-native-view-shot 4.0.3, expo-sharing, react-native-svg 15.12.1, expo-splash-screen 31.0.13 (모두 Expo Go 번들 포함).
6. 문서 미결로 기본값 정한 것들 (사용자 조정 가능): 테스트 쓰기문제 비율 30%(lib/reviewQueries.ts WRITING_RATIO), 점수→용돈 매핑(lib/incomeQueries.ts DEFAULT_INCOME_RULES), 엿보기 1.4초(app/day PEEK_DURATION_MS), 사이렌 시점 10초(app/day SIREN_AT_REMAINING_MS).
7. **개발용 도구** — 설정 화면 맨 아래 `__DEV__` 전용 섹션(**오늘 슬롯 기록 삭제**, 오늘 테스트 기록 삭제, **오늘 미루기·넘어가기 기록 삭제**, **알림 물어보기 기록 삭제**(연속 거절 카운터도 함께 0으로 — 없으면 QA 중 [괜찮아] 3번에 덮개가 영영 안 뜬다), **테스트 예약 설정 초기화**, 알림 테스트). 배포 빌드에는 나타나지 않는다. 슬롯 삭제는 오늘치 `slot_part`·`retrieval_session`·`habit_bonus`를 지운다 — "슬롯당 1회"가 DB UNIQUE 제약이라 이게 없으면 전구 점등·배너·동전·연쇄 버튼을 **다음 시간대가 열릴 때까지(최대 5시간)** 다시 볼 수 없다. 미루기·넘어가기 기록 삭제(`clearTodayTestGateState()`, 설계.md §8)는 예약 테스트 덮개의 미루기·넘어가기만 지운다 — `SNOOZE_LIMIT`회를 실기기 QA에서 다 쓰면 이게 없으면 그날은 덮개를 다시 볼 수 없다. **예약 설정 초기화**(`resetTestAlarmConfig()`)는 켬/끔·시각을 "설정한 적 없는" 상태로 되돌린다 — `setTestAlarm()`의 비대칭(켜기는 즉시, 끄기·시각 변경은 내일부터) 때문에 **한 번 켜면 같은 날에는 되돌릴 방법이 없어서**(`current.enabled`가 계속 true라 `isTurningOn`이 영영 false) QA가 하루에 한 번밖에 안 된다. 그 비대칭은 회피를 막는 제품 설계이므로 손대지 않고 개발 빌드에만 우회로를 뒀다.
8. **홈 리뉴얼 실기기 QA 1차** (2026-08-13) — 통과: 호랑이 위에서 **세로 스크롤 정상**(제스처 좌우 전용 설계가 의도대로 동작), 제스처 5종 손에 맞음, 폰트·버튼 라벨·말풍선 3줄 정상 렌더. 조정: 머리 튕김 `HEADPOP_GAP_DESIGN_PX` 30→**60**(더 확실한 분리를 원하면 90 = 갭 33px).
   - **버그 1(수정 완료)**: 잔디·전구가 3배로 커져 화면을 덮음. 게이지 안에서도 **`scale`로 px를 직접 계산한 것(빨간 점·연속 텍스트)은 제자리**였고 **부모 높이에 기댄 것(`StyleSheet.absoluteFill`+`height:'100%'`)만** 넘쳤다. 호랑이가 멀쩡했던 건 `aspectRatio`로 스스로 높이를 정해 부모에 의존하지 않기 때문. → `GrassGauge`를 전부 명시적 px(`928*scale` × `619*scale`)로 바꾸고 `overflow:'hidden'` 안전망 추가. **근본 원인(왜 부모 높이가 안 잡혔는지)은 미규명** — 같은 패턴을 쓰는 버튼·태양은 정상이었다. 고친 코드엔 부모 의존 경로가 없으므로 재발하지 않지만, **비슷한 증상이 또 나오면 이 항목을 먼저 볼 것.**
   - **교훈**: 홈 화면에서 이미지 크기는 `%`·`absoluteFill`로 부모에 기대지 말고 **`scale`로 직접 px를 계산**한다. 부모 높이가 안 잡히면 이미지가 원본 크기로 흘러넘친다.
   - 2026-08-15~16 추가 확인 완료: 스트릭 글자·말풍선 위치 튜닝, 안전영역 기준점 전환, **빨간 점 위치와 전구 탭**(누를 때 점이 빨강→오렌지). 점을 흐리게 하는 방식은 8.7pt라 안 보여서 색 변경으로 바꿨다.
   - **애니메이션 함정 2건**(2026-08-14 수정): ⓐ 머리 튕김 **복귀**를 `withSpring(overshootClamping:false)`로 두면 착지 후 출렁인다 → `withTiming` + `Easing.in(cubic)` 낙하(220ms)로 교체. ⓑ 숨쉬기를 `withRepeat(withSequence(+a, −a), -1, false)`로 짜면 **시퀀스가 시작점으로 돌아오지 않아** 매 주기 값이 리셋되며 "툭" 튄다 → `withRepeat(withTiming(−a), -1, true)`로 교체(한 방향만 정의하고 되돌아오기는 역재생에 맡긴다 — 이음새가 구조적으로 생길 수 없다).
   - **홈 이미지 13장(디코딩 22MB)이 시차를 두고 뜨는 현상** — 초기화 게이트가 DB·폰트만 기다리고 이미지는 안 기다려서다. **일단 그냥 두기로 함**(2026-08-16 사용자 판단): 지금 보이는 시차는 대부분 Expo Go가 Metro에서 파일을 네트워크로 받아오는 탓이고, 프로덕션 빌드는 에셋이 이미 기기에 있어 원인 자체가 없다. `Asset.loadAsync()`는 **다운로드만 하고 디코딩은 안 하므로** 프로덕션에선 거의 무효. **첫 EAS 빌드에서도 시차가 남으면** 그땐 원인이 디코딩이라 해법이 다르다(이미지 축소는 불가 — 이미 @3x 1:1이다).
   - 남은 확인: 글자 크기 3곳(`HomeMenuButtons`의 `label.fontSize` / `TigerHero.BUBBLE_LINES` / `GrassGauge.STREAK_TEXT.fontSize` — 사용자 지정 비율 90:80:100 / 52:140:52 / 52), 잔디 게이지 **전구 점등**(슬롯 완료 시 `bulb-glow` 표시 — 아직 미확인), 스플래시 애니메이션(Expo Go에서 가능).
9. **다음 단계 — 배포 3단계 일정** (2026-08-15 갱신). 앞 단계를 완료해야 다음 단계로 간다. 세션 간 진행 상황은 태스크 목록이 아니라 이 항목이 원본이다.

   **Apple 개발자 계정**: 2026-08-14(금) 결제 완료, **승인 대기 중**. 미국 업무시간 기준이라 8/17~18 예상. 신분증 제출 요청 메일이 올 수 있으니 스팸함까지 확인할 것(놓치면 무기한 대기).
   **번들 ID 확정**: `com.heruse.horang` (iOS `bundleIdentifier` / Android `package` 동일). **출시 후 변경 불가.**

   **[1단계] 내부 TestFlight (가족)** — 심사 없음, 개인정보처리방침 불필요
   - 딸 에셋 대기: `tiger-face-yawn1/yawn2`·`tiger-claws` → 하품 연출 연결(`TigerHero`의 `PET_LONG_TODO` 주석에 타임라인) / 사운드 4종 녹음 → `expo-audio` 설치·연결(`lib/tigerSounds.ts`가 현재 no-op)
   - ~~앱 아이콘·스플래시~~ **완료** (2026-08-16, §4-2). 스플래시 실기기 확인은 ②(애니메이션)만 Expo Go로 가능하고 ①(네이티브)은 첫 EAS 빌드에서 처음 보게 된다 — 그때 호랑이가 ①→② 사이에 튀지 않는지 볼 것.
   - 코드: 실기기 전 화면 QA(홈 잔여 항목은 §8) → `eas-cli` 설치 + `eas build:configure` → EAS 빌드 → 내부 테스터 등록(가족 각자 Apple ID를 App Store Connect 사용자로 초대, 최대 100명)
   - **`eas-cli` 설치 완료** (2026-08-21, v22.2.0). codex와 같은 이유로 **절대경로로 실행**할 것: `~/.npm-global/bin/eas` — PATH 추가가 `.zshrc`에만 있어 비대화형 셸에서는 `which eas`가 실패한다(미설치로 오판 금지). `eas login`은 대화형이라 사용자가 직접 실행한다.
   - **주의**: Xcode/Xcode Cloud의 "TestFlight Internal Only" 업로드 옵션으로 올린 빌드는 외부로 승격이 안 된다. EAS 기본값은 App Store 배포용이라 해당 없으나 `eas.json` 작성 시 확인할 것.

   **[2단계] 외부 TestFlight (지인·소수 학부모)** — **진행 중** (1단계 가족 설치·사용 완료, 2026-08-21 착수). Beta App Review 있음(정식 심사보다 가벼움, 보통 1~2일)
   - **현재 상태 (2026-08-25 갱신)**: 외부 테스터 **2명**, 외부 피드백 아직 없음. 배포 중인 것은 빌드 4(베타 심사 통과, Public Link)이고 **빌드 4에는 iOS 접근성 설정 결함이 있다**(동작 줄이기 켜면 호랑이 반응·로켓 축포가 안 보이고, 텍스트 크기를 바꾸면 홈 글자가 어긋남 — `9ddc856`에서 수정). 빌드 5는 그 수정을 담아 **업로드까지 마쳤으나 외부 그룹 배정·What to Test 작성이 남아 있다**(절차는 `TestFlight-외부테스트.md` §6).
   - **다음 외부 배포는 빌드 5가 아니라 빌드 6이 낫다** (2026-08-25 판단). 빌드 5 이후 **복습 슬롯 편입**(위 §0)에 더해 **예약 테스트 + 진입 잠금**(위 §0, 설계.md §8)까지 들어가 슬롯 규칙과 테스트 진입 경험이 함께 바뀌었다. 지금 빌드 5를 뿌리면 테스터가 곧 사라질 규칙을 보게 되는데, **발견 가능성 검증은 사람당 1회성**이라 그 표본을 되살릴 수 없다. 실기기 QA를 마치고 두 기능을 함께 담은 빌드로 한 번에 나가는 편이 값어치가 있다.
   - **발견 가능성 검증은 사람당 1회성이다.** 한 사람은 이 앱을 딱 한 번만 처음 본다. 고장 난 빌드로 첫인상을 쓰면 그 사람의 표본은 되살릴 수 없으므로(호랑이를 눌렀는데 반응이 없으면 "누르는 데가 아니구나"로 학습하고 끝난다), **테스터를 한꺼번에 다 부르지 말고 몇 명 남겨둔다.**
   - **절차·제출 문구는 `TestFlight-외부테스트.md`가 원본** — App Store Connect에 붙여넣을 Beta App Description / Review Notes / What to Test 전문과 단계별 체크리스트가 거기 있다. 여기 CLAUDE.md에 중복 기재하지 말 것.
   - 개인정보처리방침·지원 페이지 **작성 완료** (`docs/privacy.html`, `docs/index.html` — 한/영 병기). GitHub Pages(`main` 브랜치 `/docs` 폴더)로 게시하면 `https://oher77.github.io/horang/` · `.../privacy.html`. **3단계에서도 같은 URL을 쓴다.** 내용은 "데이터 미수집"이 사실임을 코드로 확인하고 썼다 — 앱 코드에 `fetch`·`axios`·URL 접근 0건, 분석/추적 SDK 없음, 알림은 로컬 전용(원격 푸시 토큰 코드 없음). 따라서 App Privacy 설문은 **Data Not Collected**로 답한다.
   - 내부에서 쓰던 **빌드를 그대로 외부 그룹에 승격**할 수 있다(재빌드 불필요). 단 TestFlight 빌드는 업로드 90일 후 만료.
   - 전환 조건: **개인정보처리방침 URL**(Test Information 필수) + 테스트 안내문 + 연락처
   - 목적: **처음 보는 중학생이 홈 화면에서 뭘 해야 할지 아는가**를 확인. 딸은 제작 과정을 봐서 앱을 이미 알기 때문에 발견 가능성 검증이 불가능하다(머리 제스처를 탭 하나로 다 발견되게 재설계한 근거와 동일한 문제).
   - 생략 가능하나 공개 출시가 목표면 거치는 게 값어치가 있다. TestFlight 빌드는 **90일 후 만료**라 이 시점엔 대개 재빌드하게 된다.

   **[3단계] App Store 공개 출시**
   - 필요: 지원(Support) URL / App Privacy 설문(데이터 미수집 신고) / 메타데이터(이름·부제·설명·키워드 100자·카테고리·연령등급) / **스크린샷** / IP 체크리스트 최종 확인 / 계정 실명 공개 여부 결론 / **데이터 백업 방식 결론**(§제품 방향 (d))
   - 앱 이름은 2026-08-16 **"호랑잉글리시"로 변경**(옛 이름 "호랑이 잉글리시" — 딸 그림의 글씨에 맞췄고, 홈 화면 아이콘 라벨도 짧을수록 안 잘린다). **새 이름 기준 App Store 중복 검색 완료** (2026-08-21, 한국 스토어) — "호랑잉글리시"·"호랑이 잉글리시"·"호랑 영어" 모두 **0건**, 이름 충돌 없음. 함께 안 바꾼 것: `bundleIdentifier`(출시 후 변경 불가) / `slug`·`scheme`(EAS 프로젝트 연결·딥링크, 사용자에게 안 보임).
   - App Store 설명 문구는 **구매 결정을 부모가 하므로 신뢰·전문 톤**(설계.md §6-14 원칙). 교육부 지정 어휘 2,416개 6개월 완주를 앞세운다.

   **[4단계] 상품성 검증 후** — 고등학생용 앱 분기(저장소 복사).
