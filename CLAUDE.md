# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

# 오케스트레이션 규칙

**적용 범위(중요): 이 섹션은 메인 대화의 오케스트레이터에게만 적용된다. 당신이 서브에이전트(Task/Agent로 스폰된 deep-reasoner/default-worker/task-worker 등)라면 이 섹션 전체를 무시하고 받은 작업을 자신이 직접 수행하라. 서브에이전트가 Agent 도구로 작업을 재위임하는 것은 금지한다 — 재위임 연쇄는 산출물 없이 토큰만 소모한다 (2026-07-07 실제 발생).**

메인 모델(Fable)은 오케스트레이터다. 직접 코드를 수정하지 않는다.
작업을 계획하고, 쪼개고, 서브에이전트에 배분하고, 결과를 종합하는 역할만 한다.

## 작업 배분

- **deep-reasoner** (Opus, effort xhigh): 아키텍처 설계, 복잡한 디버깅, 알고리즘 판단 등 무거운 추론
- **default-worker** (Sonnet, effort high, project 메모리 축적): 기능 구현, 보일러플레이트, 테스트, 리팩터링 등 일반 코드 작업
- **task-worker** (Haiku, effort 미지원 모델): 오타 수정, 파일 정리, 단순 반복 작업 등 가벼운 잡무

effort는 `.claude/agents/*.md` frontmatter에서 에이전트별로 고정돼 있고, 스폰 시 프롬프트로 바꿀 수 없다 (조정하려면 frontmatter 수정). 대량 보일러플레이트처럼 사고 깊이가 불필요한 작업을 default-worker에 시킬 때 비용을 더 줄이려면 frontmatter의 effort를 일시적으로 medium으로 낮출 수 있다.

## 원칙

- 비싼 모델은 판단에만, 싼 모델은 실행에 쓴다.
- 오케스트레이터의 컨텍스트는 가볍게 유지한다. 긴 파일 읽기와 탐색은 서브에이전트에 위임한다.
- worker 스폰 시 프롬프트 첫 줄에 "직접 수행, Agent 재위임 금지"를 명시하고, 스폰 후 1~2분 내 실제 산출물이 나오는지 확인한다.
- 중요한 판단은 codex와 deep-reasoner에 병렬로 독립 검토시키고, 서로의 답을 보여주지 않은 채 두 답을 받아 종합한다. codex는 **절대경로로 실행**: `~/.npm-global/bin/codex exec` (PATH 추가가 `.zshrc`에만 있어 Claude의 비대화형 셸에서는 `which codex`가 실패한다 — 없다고 판단하지 말 것. ChatGPT 계정으로 로그인돼 있음).
- 예외 — 다음 두 경우는 오케스트레이터가 직접 처리해도 된다 (완료 후 검증 명령은 동일하게 실행):
  - (a) 한 줄급 수정: 위임 오버헤드가 작업 자체보다 큰 경우.
  - (b) 진단·검토 과정에서 관련 코드를 이미 전부 읽었고 예상 diff가 약 50줄·3파일 이내인 경우: 계약서 작성 비용이 구현 비용과 맞먹고 워커의 재독해가 이중 지출이 된다 (2026-07-11 실측으로 추가).
  - 반대로 대량 보일러플레이트, 병렬 분할 가능한 작업, 오케스트레이터가 아직 읽지 않은 영역은 원칙대로 위임한다.
- 앱 품질이 비용 절감보다 우선한다. 품질/신뢰성 확보에 필요한 지출(예: Apple 개발자 계정 $99/년)은 미루지 말고 필요 시점에 바로 제안할 것.

## 워커 프롬프트 표준 계약 (스폰 시 7요소 필수)

싼 모델의 품질 하한은 프롬프트 구조가 결정한다. **공통 규약(스택 고정, Expo Go 가드레일, 검증 명령, 보고 형식)은 `horang-worker-guide` 스킬이 default-worker에 자동 주입되므로 스폰 프롬프트에 중복 기재하지 말 것** — 프롬프트에는 작업 고유 사항(2·3·4번 + 작업별 특이 가드레일)만 쓴다. 워커 스폰 프롬프트에 반드시 포함:

1. 첫 줄: "직접 수행하라. Agent 도구로 재위임 절대 금지."
2. 필독 문서를 **섹션 번호까지** 지정 (예: "설계.md §1.3, §4.5"). 문서 전체를 읽게 하지 말 것.
3. 산출물 명세: 파일 경로·인터페이스(플레이스홀더, 스키마, 시그니처)를 계약으로 고정.
4. 파일 소유권 경계: 수정 가능/금지 목록 명시 (병렬 워커 충돌 방지의 핵심).
5. 검증 명령 목록: 실행할 명령을 그대로 적시 (예: `npx tsc --noEmit`, `npx expo export`).
6. 완료 보고 형식 강제: **"스펙과 다르게 판단한 것" 섹션 의무화.** 약한 모델의 최대 리스크는 틀리는 게 아니라 조용히 틀리는 것 — 편차 보고를 강제해 오케스트레이터가 잡는다.
7. 모호하면 중단·보고: 임의 해석 금지, 선택지를 제시하게 할 것.

## 검증자 분리

중요 구현 완료 시 구현 워커의 자기검토에 의존하지 않는다. 신선한 컨텍스트의 별도 검증자(Haiku/Sonnet)를 스폰해 "지시서의 검증 항목을 실제로 통과하는지"만 확인시킨다. 구현자와 검증자는 같은 에이전트를 재사용하지 않는다 (신선한 눈이 자기검토보다 잘 잡는다).

## 실패 사례 체크리스트 (재발 방지 — 사고가 나면 여기에 추가)

- **재위임 연쇄** (2026-07-07): 워커가 Agent로 재위임 → 산출물 없이 토큰 소모. → 표준 계약 1번 + 스폰 후 1~2분 내 산출물 확인으로 방지.
- **실행 환경 호환 미검증** (2026-07-07): npm latest(Expo SDK 57)로 스캐폴드했으나 App Store의 Expo Go는 SDK 54까지만 지원 → 실기기 테스트 불가, 전체 다운그레이드로 복구. → 새 스택 도입/스캐폴드 시 개발 루프의 실행 환경(스토어 앱 버전, OS 최소사양 등)과의 호환을 **먼저** 검증한다. npm "latest"는 실행 환경보다 앞설 수 있다.
- **환경변수 전달 오해** (2026-07-07): 사용자 터미널의 `export`는 Claude의 셸에 전달되지 않는다 (별도 프로세스). → 환경변수는 `.claude/settings.local.json`의 `env`에 저장한다.
- **PATH 차이로 CLI 미설치 오판** (2026-07-09): `which codex` 실패를 "미설치"로 판단했으나 실제로는 `~/.npm-global/bin/codex`에 존재 — PATH 추가가 `.zshrc`에만 있어 비대화형 셸이 못 읽은 것. → 사용자가 쓴다고 알려진 CLI는 `which` 실패 시 `~/.npm-global/bin` 등 흔한 설치 경로를 확인하고 절대경로로 실행한다.

## 기계적 검증 게이트 (.claude/settings.json — 모델의 선의에 의존하지 않는 층)

- 원본 보호: `data/**`, `build/id_map.json`, `단어장 앱 만들기.md`는 Edit/Write가 permissions.deny로 차단됨 (clean.py 등 스크립트 경유 쓰기는 허용).
- 자동 타입체크: 턴 종료 시(Stop/SubagentStop 훅, `.claude/hooks/tsc-check.sh`) 수정된 `.ts/.tsx`가 있으면 `tsc --noEmit`을 1회 실행, 실패 시 모델에게 자동 통보. 워커가 검증을 깜빡해도 시스템이 잡는다. (편집마다 검사하던 방식은 다중 Edit 중간 상태를 잡는 오탐이 잦아 2026-07-11 턴 종료 검사로 이동.)

# 프로젝트: 호랑이 잉글리시 (Horang English)

고등학생 대상 영단어 학습 앱 (React Native + Expo). 약 2400개 단어를 하루 단위 단어장(Day1, Day2...)으로 생성해 암기·복습·테스트하고, 점수를 용돈(Income) 장부와 연결한다.

## 필독 문서

- `설계.md` — **구현의 유일한 지침.** SQLite DDL 전문, 원본 정제·파싱 규칙, 콘텐츠 생성 파이프라인, 화면 구조·라우팅·상태관리, 핵심 쿼리, 미결 목록(§6). 코드 작업 전 반드시 해당 섹션을 읽을 것.
- `기술방향.md` — 스택 확정 근거(PWA 반려 사유), 개발 환경 제약, 비용 전략, 가드레일
- `단어장 앱 만들기.md` — 기획서 원문 (화면별 요구사항). **수정 금지 — 사용자의 원본 문서.**

## 명령어

```bash
npx expo start          # Metro 개발 서버 (Expo Go로 iPhone 실기기 테스트, QR 연결)
python3 scripts/clean.py  # 원본 정제 파이프라인: data/voca_data.xlsx → build/ + review/ (인자 없음, 멱등)
```

- 배치 생성 파이프라인(제출~content.db 빌드) 절차는 `/content-pipeline` 스킬 참조 (매 세션 로드 안 됨 — 필요할 때만).

- 테스트·린트는 아직 미설정 (스캐폴드 직후 상태).
- Expo SDK 54 / RN 0.81 / React 19.1 / TypeScript 5.9. 로컬 Node v22.
- iOS 네이티브 빌드는 로컬 불가(iMac 2015, Xcode 14.2 한계) — EAS 클라우드 빌드 사용. 개발 반복은 Expo Go로 무료.

## 아키텍처

**2-DB 구조** (설계.md §1): 읽기 전용 `content.db`(단어·뜻·예문, 사전 빌드해 에셋 번들) + `user.db`(학습 진행·점수·설정, 첫 실행 시 생성). 날짜는 INTEGER epoch day(**디바이스 로컬 자정** 기준 일수, lib/dates.ts `toEpochDay()` 단일 유틸만 사용)로 저장 — 복습 -N일 조회가 정수 비교로 인덱스를 탄다.

**콘텐츠 데이터 흐름** (설계.md §2–3):
```
data/voca_data.xlsx (원본, 수정 금지)
  → scripts/clean.py (정제·파생어 분해·쓰기 파싱·stable_id 발급)
    → build/words.json (words 2417 / lemma_groups 2171 / writing_items 43)
    → build/id_map.json (stable_id 영속 맵 — 재실행 멱등성의 핵심, §3.4)
    → review/*.csv (사람 검수 큐: 목표 영단어 미확정 항목)
  → [검수 승인 후] LLM 뜻·예문 일괄 생성 → content.db 빌드
```

**앱 코드**: expo-router 파일 기반 라우팅(`app/`), 상태관리는 Zustand + 화면 로컬(React Query 등 미도입). 화면 트리·화면별 쿼리 매핑은 설계.md §4, 폴더 지도는 `구조.md`.

## 가드레일

- **Expo Go에 포함되지 않은 서드파티 네이티브 모듈 도입 금지.** 도입하는 순간 dev build(= 유료 Apple 계정)가 강제됨. FlashList도 이 이유로 금지 — 100+행 테이블은 RN 내장 FlatList 윈도잉(보이는 15~20행) + stagger 애니메이션으로 처리 (설계.md §4.5).
- 허용된 네이티브 의존성: expo-sqlite, expo-speech, expo-notifications, react-native-reanimated, react-native-gesture-handler, react-native-view-shot, expo-sharing, react-native-svg (+ expo-router 동반 패키지). 홈 리뉴얼에서 추가 예정(둘 다 bundledNativeModules 확인 완료): **expo-audio ~1.1.1**(호랑이 사운드 — `expo-av`는 SDK 54에서 deprecated이므로 쓰지 말 것), **expo-haptics ~15.0.8**. 추가 전 `node_modules/expo/bundledNativeModules.json`에 해당 모듈이 있는지 반드시 확인할 것 — 없으면 dev build가 강제된다.
- `data/voca_data.xlsx`·`build/id_map.json`을 임의 수정/삭제하지 말 것 — id_map이 깨지면 앱 업데이트 시 사용자 진행 데이터 참조가 전부 무효화된다.
- TTS 연속 탭 중복 재생 방지: `Speech.stop()` 후 `speak()` 패턴.

## 핵심 도메인 규칙

- 단어장은 하루 1개만 생성, 전체 Day 간 단어 중복 절대 금지 (미사용 풀에서 랜덤 추출)
- 파생어는 표제어와 동등한 독립 단어 항목 (노출·출제 동일 취급, 쌍 관계는 lemma_group 메타데이터)
- 복습 스케줄: 학습일 기준 -1/-3/-7/-14/-30/-60/-120일 전 단어장
- 테스트는 자기채점(학생이 직접 오답·발음헷갈림 체크), 혼합 출제(단어→뜻/뜻→단어/쓰기 문제), **하루 1회만**(taken_day 기준, 재채점은 허용 — 2026-07-12 확정)
- 발음 기능은 TTS 출력 + 체크 장부만 (녹음 없음)
- 기억 인출 실패 5단계: 우스와이프 증가/좌스와이프 감소 (아이의 자기 표시 — 뱃지·세션 임계값에만 쓰임. **업적의 낯가림 Top10은 이와 별개로 테스트 기준**: 최신 결과가 오답인 단어를 누적 오답 횟수순으로, 외우면 자동 제외 — 혼동 주의)
- Income: 점수별 금액을 표시하고 부모 지급 여부만 체크하는 장부 (결제 연동 없음)

## 제품 방향 (2026-08-10 확정 — 상세는 설계.md §6-15)

**이 앱은 중학생용 단일 제품이다.** 교육부 지정 어휘 2,416개를 **6개월 안에 완주**하는 것 자체가 소구 포인트이므로(하루 20개 × 121일, 주 5일 기준 약 5.6개월) **단어 풀을 확장하지 않는다.**

**고등학생용은 별도 앱**으로 뺀다 — 더 어려운 단어 풀 + 독해 중 만난 단어 직접 추가(LLM 생성). 착수는 **중학생용 출시 후 상품성 검증 뒤**, 이 저장소를 복사해 분기한다. 디자인 2종 병행·테마 전환·브랜치 관리는 전부 폐기.

## 현재 상태 & 다음 단계 (2026-08-10 — 현재 사실만 기록, 경위·이력은 git log 참조)

0. **인출 습관 시스템** (설계.md §7이 확정 스펙) — 구현 완료, 실기기 QA 대기. 홈 4슬롯 게이지·스트릭, 단어장 세션 트래킹(체류시간 판정), 학습/인출모드(카운트다운 라인바·시간임박 사이렌·타임오버 탭 재시작, 복습 진입 시 인출모드 기본), 슬롯 알림(lib/notifications.ts, 기본 OFF), 습관 보너스 장부(설정 화면에서 금액 편집), 업적 차트 3종(lib/statsQueries.ts). user.db 스키마 버전 '3' (IF NOT EXISTS 멱등 마이그레이션). 판정식·시간 상수는 코드가 원본: app/day/[dayId].tsx 상단 상수, lib/habitQueries.ts.
1. **콘텐츠 완성** — `assets/db/content.db` 실데이터 빌드 완료 (word 2,416 / meaning 3,719 / example 11,411 / writing 43, 2.9MB). 검수·배치 생성·validate 전 항목 통과. 파이프라인 절차는 `/content-pipeline` 스킬 참조.
2. **화면 구현 완료** — 홈·단어장(가리기/스와이프 5단계/단어 셀 탭 TTS/예문 바텀시트)·복습·테스트·용돈 장부·발음 체크 장부(셀프 해소+재체크 자동 복귀)·설정·**자랑하기 리포트**. Expo Go 실기기 구동 확인됨.
3. **자랑하기 리포트** (설계.md §6-14) — 업적 화면 상단 "🎉 자랑하기" → `app/brag`. 성장 지표를 포스터 1장으로 캡처해 카톡 공유(react-native-view-shot + expo-sharing). 히어로=암기 자산 곡선(react-native-svg 라인 차트), 나머지는 2열 그리드에 **우선순위 상위 4개만** 노출. 발사 후 로켓 축포 애니메이션. 문구 톤 원칙: **아이가 조작하는 UI=재미 / 부모가 받아보는 산출물=신뢰·전문**, 부모 지칭은 "부모님".
4. **홈 화면 리뉴얼 진행 중** — 딸이 그린 시안으로 교체. **지침은 `design/홈화면-에셋-가이드.md`가 원본**(시안: `design/호랑이잉글리시.png`, 아빠 시안 일체는 `design/고등학생용-보류/`). 확정: 진입점은 태양=설정 / 말풍선=오늘 단어장 / 호랑이 머리=사운드·애니메이션 / 꽃=업적 / 리본=획득물(신규, 라우팅만 먼저) / 복습·발음체크·테스트. 시안이 화면보다 32% 길어 **세로 스크롤 허용**(첫 화면에 태양·호랑이·게이지·버튼 3개까지는 보이게). 잔디 게이지는 3층 스택 — 잔디 배경 → 빛(완료 슬롯만) → 전구 4개 통이미지(윤곽선만, 속 투명) → 빨간 점(현재 슬롯, 코드로 그림). 전구 중심은 폭의 12.5/37.5/62.5/87.5%. 에셋은 `tiger_hero.png`(1060×1111, **모든 호랑이 레이어의 기준 캔버스**)·태양·산맥까지 도착. **호랑이는 머리를 분리한다**(쓰다듬기·하품 때문): `tiger-body.png`(몸) + 머리 컨테이너(`tiger-face-*.png` + `tiger-eye-shut.png`). 전부 1060×1111 캔버스 유지 → 좌표 계산 없음. 머리 회전축 = **(532, 285)** = 50.2%/25.7%. 터치 영역 실측(겹침 없음, 103px 여유): **머리 제스처** x 359–697 / y 0–350, **말풍선 탭**(단어장 진입) x 90–970 / y 384–954 — 제스처 5종은 머리 영역 안에서만 잡힌다. 터치 영역은 **애니메이션 컨테이너 밖에 고정**(머리를 따라 움직이면 추적이 꼬임 — 제스처는 누르는 순간 캡처되므로 머리가 빠져나가도 정상 작동). **깜박임은 덮개(`tiger-eye-shut.png`), 표정은 통스왑 — 섞어 쓴다**: 깜박임은 고빈도·저변화라 통스왑 시 윤곽 1px 차이가 계속 떨려 보이고, 하품은 저빈도·고변화라 묻힌다. 그래서 `tiger-face-blink.png`는 만들지 않는다. 덮개는 **색을 칠하지 말고 옆 털을 복사해 덮을 것**(크레용 질감이라 평평한 주황은 경계가 드러남), 페이드 금지(110ms 하드컷). **머리 이벤트는 랜덤이 아니라 제스처별 1:1**(2026-08-11, 랜덤안 폐기 — 아이가 인과관계를 배워야 반복해서 만짐): 탭1회=움찔+랜덤사운드 / 탭3연타(1.2s내)=갸우뚱+으응? / 좌우쓰다듬기=머리내려감+그르렁 / 계속쓰다듬기1.5초↑=하품+손톱+어흥 / **길게눌렀다떼기**=눌렸다튕기며분리+꺅~. 평상시엔 간헐적 눈깜박만. **트리거는 의도적으로 쉬운 것만 골랐다**(가이드 §2-8) — 연출·사운드가 재미를 만들지 제스처의 정교함이 만드는 게 아니다. 폐기한 안과 이유: ⓐ "아래로 당겨 1초 유지"는 멈춤 판정(속도 임계값+디바운스)이 필요하고 **홈이 세로 스크롤이라 ScrollView가 먼저 가로채** `scrollEnabled` 토글까지 필요 → `Gesture.LongPress()`로 대체(제자리 누름은 스크롤을 안 건드림), ⓑ 쓰다듬기 "횟수" 카운팅은 누적거리+판정지연이 필요 → **"시간"으로 대체**(계속 쓰다듬으면 그르렁→하품, 고양이처럼 자연스럽고 "그르렁 3번 뒤 하품" 문제가 원천 소멸), ⓒ 탭 350ms 대기 → 이전 소리 stop 후 재생(기존 TTS 패턴과 동일). 쓰다듬기 Pan은 **좌우 전용**(`activeOffsetX: ±14` + `failOffsetY: ±10` → 수평 ±36°, `tanθ < 10/14`)으로 세로 스크롤과 분리 — 세로 쓰다듬기는 기술적으로 가능하지만 호랑이 위에서 스크롤이 막혀 폐기(호랑이가 첫 화면 큰 영역을 차지 + 시안이 32% 길어 아래를 못 찾게 됨). 45°가 아니라 36°인 이유는 오판의 대가가 비대칭이기 때문(스크롤→쓰다듬기 오판은 짜증, 반대는 다시 하면 됨). 1초 도달 신호는 `expo-haptics`(손가락이 머리를 가려 시각 신호 무용). 이벤트 1은 `yawn`에 감은 눈이 있으므로 덮개를 꺼야 한다. **하품만 유일하게 그림 교체(불연속)라 한 장 스왑으로는 안 된다** — 나머지 3개는 transform이라 저절로 연속. 입 2단계(`yawn1`/`yawn2`, 닫힐 때도 `yawn1` 경유) + 머리 뒤로 젖히는 모션을 겹쳐야 하품으로 읽힌다(타임라인은 가이드 §2-7). 이벤트 4 때문에 **목 연장분 상단이 화면에 드러나므로 둥글게 마감 필수**(60px 연장 + 90px 상승 = 30px 갭). 사운드는 `expo-audio`(Expo Go 번들 포함, `expo-av`는 deprecated), iOS 무음 스위치 대비 `playsInSilentMode: true` 필요. 미제작: `tiger-body`, `tiger-face-normal/yawn1/yawn2`, `tiger-eye-shut`, `tiger-claws`, 사운드 4종(딸 육성 녹음 권장).
5. 프로젝트는 **Expo SDK 54 고정** (스토어 Expo Go 호환 — 상세는 horang-worker-guide 스킬). 시스템 Python 3.9. anthropic SDK 0.116.0 --user 설치됨. 추가 네이티브 의존성: react-native-view-shot 4.0.3, expo-sharing, react-native-svg 15.12.1 (모두 Expo Go 번들 포함).
6. 문서 미결로 기본값 정한 것들 (사용자 조정 가능): 테스트 쓰기문제 비율 30%(lib/reviewQueries.ts WRITING_RATIO), 점수→용돈 매핑(lib/incomeQueries.ts DEFAULT_INCOME_RULES), 엿보기 1.4초(app/day PEEK_DURATION_MS), 사이렌 시점 10초(app/day SIREN_AT_REMAINING_MS).
7. **개발용 도구** — 설정 화면 맨 아래 `__DEV__` 전용 섹션(오늘 테스트 기록 삭제, 알림 테스트). 배포 빌드에는 나타나지 않는다.
8. **다음 단계**: 홈 화면 에셋 완성 → 홈 리뉴얼 구현 → 실기기 전 화면 QA → Apple 개발자 등록($99/년, 미완료) → EAS 빌드 → TestFlight 가족 배포 → 상품성 검증.
